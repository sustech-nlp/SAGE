"""Data preprocessing for SAGE.

* :mod:`sage.data.spider` — Spider / Spider-Realistic / Spider-DK preprocessing.
* :mod:`sage.data.bird` — BIRD train / dev preprocessing.
* :mod:`sage.data.schema` — common helpers (CSV / description / hardness).
* :mod:`sage.data.spider_eval` — vendored Spider evaluation suite
  (Yu et al., 2018) used for hardness scoring.

Each dataset module is executable as ``python -m sage.data.{spider,bird}``.
"""
